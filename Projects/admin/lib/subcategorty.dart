import 'package:flutter/material.dart';

class Subcategorty extends StatelessWidget {
  const Subcategorty({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("subcategory", style: TextStyle(color: Colors.white),),
    );
  }
}